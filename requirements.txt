# Core — everything the pipeline needs to run.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9
httpx>=0.27
mido>=1.3
pyphen>=0.15
numpy>=1.26

# System requirement, not pip: ffmpeg and ffprobe must be on PATH.
#   macOS   brew install ffmpeg
#   Debian  apt install ffmpeg

# Optional, only for the local providers that use them:
# demucs>=4.0          # separate/demucs
# whisperx>=3.1        # transcribe/whisperx
